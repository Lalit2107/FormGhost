name: Supabase Keepalive

on:
  schedule:
    - cron: '0 8 * * *'  # Runs daily at 8:00 AM UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -sf ${{ secrets.BACKEND_URL }}/api/profile || echo "Ping failed, but workflow will continue"
